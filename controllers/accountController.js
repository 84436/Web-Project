const express = require('express')
const dbAccount = require('../models/accountModel')
const express_handlebars = require('express-handlebars')
const path = require('path')
const app = express()

module.exports = app
